# Dev inherits everything from base values.yaml
# We can just override the global domain here explicitly if needed.
global:
  domain: yas.lethanhcong.site
  serviceType: ClusterIP
services:
  storefront:
    tag: bef7aae2a088d6df7491cac38b91f9b5db831257
